package main.java.com.usher.DataStructure.stack;

/**
 * 栈，基于链表实现需要使用链表的头插法来实现，
 * 因为头插法中最后压入栈的元素在链表的开头，它的 next 指针指向前一个压入栈的元素，
 * 在弹出元素使就可以通过 next 指针遍历到前一个压入栈的元素从而让这个元素称为新的栈顶元素。
 *
 */
public class LinkedStack {
}
